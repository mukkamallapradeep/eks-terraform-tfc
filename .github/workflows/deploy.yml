name: Deploy EKS via Terraform Cloud

on:
  push:
    branches: ["main"]

# Prevent multiple runs from racing on the same ref
concurrency:
  group: tfc-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 1) Upload Terraform configuration to TFC, capture configuration_version_id
      - name: Upload configuration to TFC
        id: upload
        uses: hashicorp/tfc-workflows-github/actions/upload-configuration@v1.3.2
        with:
          organization: Poc05-Organization
          workspace: eks-terraform-tfc
          token: ${{ secrets.TFC_TOKEN }}
          directory: '.'         # <— REQUIRED: path to your TF code ('.' = repo root)

      # 2) Create a non-speculative (apply-able) run.
      #    NOTE: create-run waits until the plan reaches a terminal state.
      - name: Create applyable run
        id: create
        uses: hashicorp/tfc-workflows-github/actions/create-run@v1.3.2
        with:
          organization: Poc05-Organization
          workspace: eks-terraform-tfc
          token: ${{ secrets.TFC_TOKEN }}
          message: "Apply from push to main"
          configuration_version: ${{ steps.upload.outputs.configuration_version_id }}
          plan_only: false
          is_destroy: false

      # 3) Apply only if the run is awaiting confirmation (i.e., Auto Apply is OFF).
      - name: Apply run
        if: ${{ steps.create.outputs.is_confirmable == 'true' }}
        uses: hashicorp/tfc-workflows-github/actions/apply-run@v1.3.2
        with:
          organization: Poc05-Organization
          token: ${{ secrets.TFC_TOKEN }}
          run: ${{ steps.create.outputs.run_id }}
          comment: "Approved by GitHub Actions"
