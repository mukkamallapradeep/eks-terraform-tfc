name: Deploy EKS via Terraform Cloud

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 1) Upload your TF configuration to the TFC workspace
      - name: Upload configuration to TFC
        uses: hashicorp/tfc-workflows-github/actions/upload-configuration@v1.3.2
        with:
          organization: Poc05-Organization
          workspace: eks-terraform-tfc
          token: ${{ secrets.TFC_TOKEN }}

      # 2) Create an apply-able run in TFC (plan completes and waits for confirmation)
      - name: Create applyable run
        id: create
        uses: hashicorp/tfc-workflows-github/actions/create-run@v1.3.2
        with:
          organization: Poc05-Organization
          workspace: eks-terraform-tfc
          token: ${{ secrets.TFC_TOKEN }}
          message: "Apply from push to main"

      # 3) Apply that run in TFC
      # NOTE: 'apply-run' requires 'run', not 'run_id'; do not pass 'workspace' here
      - name: Apply run
        uses: hashicorp/tfc-workflows-github/actions/apply-run@v1.3.2
        with:
          organization: Poc05-Organization
          token: ${{ secrets.TFC_TOKEN }}
          run: ${{ steps.create.outputs.run_id }}
          comment: "Approved by GitHub Actions"
